import 'package:flutter/material.dart';

class Bike4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'BIKE 4',
        style: TextStyle(
          fontSize: 32,
        ),
      ),
    );
  }
}
